---
const { title,  subTitle } = Astro.props
---

<section class="max-w-screen-2xl mx-auto my-20">
    <div class="flex flex-col items-center text-center px-4">
        <h2 class="text-5xl text-[--main-color]  font-semibold after:content-[''] after:w-[100px] after:h-[7px] after:bg-orange-500 flex flex-col items-center gap-2 after:rounded-md">{title}</h2>
        <p class="text-lg mt-4">{subTitle}</p>
    </div>
</section>